[tool.poetry]
name = "chem-dummy"
version = "0.1.0"
description = "This package contains information about periodic elements and their properties"
authors = ["Prithivee7 <getprithivee@gmail.com>"]
readme = "README.md"
packages = [{include = "chem_dummy"}]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]
pandas = "^2.0.1"
pytest = "^5.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
