from setuptools import setup, find_packages

setup(
    name='my-model',
    version='0.3',
    author='ls',
    author_email='your.email@example.com',
    packages=find_packages(),
    install_requires=[

        # 其他依赖
    ],
)
