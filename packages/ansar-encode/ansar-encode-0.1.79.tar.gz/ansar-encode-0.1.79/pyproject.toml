[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "ansar-encode"
readme = "README.md"
authors = [
  { name="Scott Woods", email="scott.18.ansar@gmail.com" },
]
requires-python = ">=3.7"
dynamic = ["description", "version", "entry-points"]
