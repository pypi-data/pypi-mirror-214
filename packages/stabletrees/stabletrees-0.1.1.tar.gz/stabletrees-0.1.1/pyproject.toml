[build-system]
requires = ["setuptools>=61.0", "wheel", "pybind11~=2.8.0", "setuptools_scm>=6.2"]

build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
before-all = ["git clone https://gitlab.com/libeigen/eigen.git"]
