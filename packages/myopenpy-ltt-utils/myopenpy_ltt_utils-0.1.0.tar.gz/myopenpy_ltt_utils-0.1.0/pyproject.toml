[tool.poetry]
name = "myopenpy-ltt-utils"
version = "0.1.0"
description = "just for me test"
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [{include = "myopenpy_ltt_utils"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
