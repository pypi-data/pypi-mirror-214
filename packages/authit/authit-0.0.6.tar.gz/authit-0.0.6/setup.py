from setuptools import setup

setup(
    name='authit',
    version='0.0.6',
    description='An identity platform',
    author='Muthu',
    author_email='contact@muthupandian.in',
    packages=['authit'],
    install_requires=[
        "bcrypt"
    ],
)
