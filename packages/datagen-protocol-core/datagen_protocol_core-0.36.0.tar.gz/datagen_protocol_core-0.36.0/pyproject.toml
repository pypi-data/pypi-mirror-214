[tool.poetry]
name = "datagen-protocol-core"
version = "0.36.0"
description = "Datagen Protocol Core"
authors = ["ShayZ <shay.zilberman@datagen.tech>"]
packages = [
    {include = "datagen_protocol"},
    {include = "utils"},
]

[tool.poetry.dependencies]
python = "^3.8"
pydantic = "*"
dynaconf = "2.2.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
