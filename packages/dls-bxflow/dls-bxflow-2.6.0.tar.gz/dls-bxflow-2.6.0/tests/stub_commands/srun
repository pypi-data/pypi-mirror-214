#!/usr/bin/env python

import logging
import sys

logging.basicConfig(format="%(levelname)s:%(message)s", level=logging.DEBUG)

# srun ptyrex_recon -c {ptyrex_configfile}

sys.stderr.write("output from stub srun script to stderr\n")

with open("ptyrex.log", "w") as stream:
    stream.write("output from stub srun script to ptyrex.log\n")

sys.exit(0)
