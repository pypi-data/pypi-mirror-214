// Implement the logic of the bxflow pages.

class Bxflow__Page extends DlsCommon__Base {
    constructor(runtime) {
        super(runtime);
    }

    // -------------------------------------------------------------
    // Activate things on the page.

    activate() {

    } // end method
}
