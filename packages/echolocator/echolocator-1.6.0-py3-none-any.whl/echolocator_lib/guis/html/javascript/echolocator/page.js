// Implement the logic of the echolocator pages.

class echolocator__Page extends common__Base {
    constructor(runtime) {
        super(runtime);
    }

    // -------------------------------------------------------------
    // Activate things on the page.

    activate() {

    } // end method
}
