(function(){if(!window.Djblets){window.Djblets={}}"use strict";Djblets.Extension=Backbone.Model.extend({defaults:{id:null,name:null,settings:{}},initialize(){this.hooks=[]}});"use strict";Djblets.ExtensionHook=Backbone.Model.extend({hookPoint:null,defaults:{extension:null},initialize(){const extension=this.get("extension");console.assert(this.hookPoint,"This ExtensionHook subclass must define hookPoint");console.assert(extension,"An Extension instance must be passed to ExtensionHook");extension.hooks.push(this);this.hookPoint.addHook(this);this.setUpHook()},setUpHook(){}},{each(cb,context){_.each(this.prototype.hookPoint.hooks,cb,context)}});"use strict";Djblets.ExtensionHookPoint=Backbone.Model.extend({initialize(){this.hooks=[]},addHook(hook){this.hooks.push(hook)}})}).call(this);
